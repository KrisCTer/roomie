server:
  port: 8086
  servlet:
    context-path: /billing

spring:
  application:
    name: billing-service

  data:
    mongodb:
      uri: mongodb://admin:roomie123@localhost:27017/roomie_billing?authSource=admin

    redis:
      host: localhost
      port: 6379
      password: roomie123
      timeout: 60000

  cache:
    type: redis
    redis:
      time-to-live: 300000

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: billing-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: romie.housing.tech@gmail.com
    password: mznl wclo ddwt jrut
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

billing:
  overdue-check-cron: "0 0 3 * * *"
  validation:
    max-electricity-consumption: 10000  # kWh
    max-water-consumption: 1000
  # Payment Configuration
  payment:
    momo:
      partnerCode: ${MOMO_PARTNER_CODE:MOMORZW420251202_TEST}
      accessKey: ${MOMO_ACCESS_KEY:BeMUhv9rHCRg517u}
      secretKey: ${MOMO_SECRET_KEY:nl90qNFWwXrVOkf9uDWsYPUZ4bIr4gTb}
      endpoint: ${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api/create}
      returnUrl: ${MOMO_RETURN_URL:http://localhost:3000/payment/return}
      notifyUrl: ${MOMO_NOTIFY_URL:http://localhost:8086/billing/webhook/momo}

    bank:
      code: "VCB"  # Mã ngân hàng (VCB = Vietcombank)
      account: "1234567890"  # Số tài khoản
      name: "ROOMIE TECHNOLOGIES LTD"  # Tên chủ tài khoản
ocr:
  tesseract:
    datapath: /usr/share/tesseract-ocr/4.00/tessdata
  confidence-threshold: 0.7
  google-vision:
    enabled: false
    project-id: roomie-project
    credentials-path: /path/to/credentials.json

app:
  services:
    contract: http://localhost:8085/contract
    property: http://localhost:8083/property
    files: http://localhost:8088/file