package com.roomie.services.billing_service.enums;

public enum ContractStatus {
    DRAFT,
    PENDING_SIGNATURE,
    PENDING_PAYMENT,
    ACTIVE,
    PAUSED,
    TERMINATED,
    EXPIRED,
    RENEWED
}
