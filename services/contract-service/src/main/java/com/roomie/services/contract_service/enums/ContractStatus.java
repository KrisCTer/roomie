package com.roomie.services.contract_service.enums;

public enum ContractStatus {
    DRAFT,
    PENDING_SIGNATURE,
    PENDING_PAYMENT,
    ACTIVE,
    PAUSED,
    TERMINATED,
    EXPIRED,
    RENEWED
}
