server:
  port: 8083
  servlet:
    context-path: /property

spring:
  application:
    name: property-service

  data:
    mongodb:
      uri: mongodb://admin:${MONGO_PASSWORD}@localhost:27017/roomie_property?authSource=admin
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD}
    elasticsearch:
      uris: http://localhost:9200

  cache:
    type: redis

  main:
    allow-bean-definition-overriding: true

  jackson:
    default-property-inclusion: non_null

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

property:
  max-images: 20
  image-max-size: 10MB
  google-maps-api-key: your-google-maps-api-key

app:
  services:
    profile: http://localhost:8082/profile
